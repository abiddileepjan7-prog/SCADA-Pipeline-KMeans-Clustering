# SCADA-Pipeline-KMeans-Clustering
An unsupervised machine learning project that applies K-Means clustering to SCADA pipeline sensor data for discovering operational patterns and identifying abnormal pipeline behavior.
